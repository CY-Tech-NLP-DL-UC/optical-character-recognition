README

You will probably need to do some installations:

pip3 install --upgrade pip
pip3 install opencv-python
pip3 install editdistance
pip3 install tensorflow



Call the translateHandwriting(imagePath) function :

import :
from TranslateHandwriting import translateHandwriting

call:
result = translateHandwriting('../data/test.png')
or any other path to an image you want to translate


The result will appear in terminal and is stored in the variable result


For an example, see "example.py" and run 
python3 example.py
from the /src directory
